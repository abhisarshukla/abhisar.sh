---
export interface Props {
    title: string;
    description?: string;
    circleSize?: number;
    class?: string;
    footer?: string;
    href?: string;
}

const {
    title,
    description,
    circleSize = 400,
    class: className,
    href,
    footer,
} = Astro.props;

const Tag = href ? 'a' : 'div';
---

<Tag
    href={href}
    class="gradient-card group relative transform-gpu overflow-hidden rounded-[14px] bg-white/[0.05] border border-white/[0.07] p-2 transition-all hover:border-white/[0.14] hover:bg-white/[0.07] active:scale-[0.98] block no-underline"
>
    <div
        class="gradient-circle -translate-x-1/2 -translate-y-1/2 absolute transform-gpu rounded-full transition-transform duration-500 group-hover:scale-[3] opacity-0"
        style={{
            maskImage: `radial-gradient(${circleSize / 2}px circle at center, white, transparent)`,
            width: `${circleSize}px`,
            height: `${circleSize}px`,
            background:
                "linear-gradient(135deg, #3BC4F2, #7A69F9,#F26378,#F5833F)",
        }}
    >
    </div>
    <div
        class="absolute inset-px rounded-[13px] bg-[#131211]/80"
    >
    </div>
    <slot />
    <div class="relative px-4 pt-4 pb-2">
        <h3 class="font-medium text-base text-[#e8e4de] leading-snug tracking-[-0.01em]">
            {title}
        </h3>
        {
            description && (
                <p class="mt-1.5 text-[13px] font-light text-[#7a7672] leading-[1.6]">
                    {description}
                </p>
            )
        }
    </div>
    {
        (footer || Astro.slots.has("footer")) && (
            <div class="relative px-4 pb-4 pt-2 border-t border-white/[0.06]">
                {footer && (
                    <div class="font-mono text-[11px] text-[#635f5c] tracking-[0.03em]">
                        {footer}
                    </div>
                )}
                <slot name="footer" />
            </div>
        )
    }
</Tag>

<script is:inline>
    // Mouse tracking functionality
    function initGradientCard() {
        const cards = document.querySelectorAll(".gradient-card");

        cards.forEach((card) => {
            const circle = card.querySelector(".gradient-circle");

            card.addEventListener("mousemove", (e) => {
                const rect = card.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;

                if (circle) {
                    circle.style.left = `${x}px`;
                    circle.style.top = `${y}px`;
                    circle.style.opacity = "1";
                }
            });

            card.addEventListener("mouseleave", () => {
                if (circle) {
                    circle.style.opacity = "0";
                }
            });
        });
    }

    // Initialize on page load
    if (document.readyState === "loading") {
        document.addEventListener("DOMContentLoaded", initGradientCard);
    } else {
        initGradientCard();
    }

    // Re-initialize for dynamic content (useful for client-side navigation)
    document.addEventListener("astro:page-load", initGradientCard);
</script>
