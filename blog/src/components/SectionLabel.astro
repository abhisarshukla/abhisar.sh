---
interface Props {
    label: string;
}

const { label } = Astro.props;
---

<div class="flex items-center gap-3">
    <span class="font-mono text-[10px] font-medium text-ink-8 tracking-[0.12em] uppercase">
        {label}
    </span>
    <div class="flex-1 h-px bg-rim-3"></div>
</div>
